# Repository Readiness Report — DeepaBMS (Release Engineering)

**Date:** 2026-07-14
**Role:** Release Engineering Lead
**Proposed release:** v1.0.1 (v1.0.0 tag already exists and is incomplete —
it predates the P10.1 hardening fixes; per release rules a new tag `v1.0.1` is
created rather than force-moving `v1.0.0`).

## 1. Repository state (executable)

| Check | Value |
|-------|-------|
| Branch | `main` |
| Remote | `origin` → `https://github.com/00AstroGit00/DeepaBMS.git` |
| `gh` CLI | v2.96.0, authenticated as `00AstroGit00` (token present) |
| Network | `git ls-remote` succeeded → GitHub reachable |
| Tags | `2.0` (stray), `v1.0.0` (exists, incomplete) |
| Pending changes | **57** (uncommitted) |
| Version (root/backend/helm) | `1.0.0` consistent |

## 2. Pending change inventory (57 files)

- **License / metadata:** `LICENSE` (new, MIT), `README.md`, `package.json`,
  `apps/backend/package.json` (`"license":"MIT"`).
- **Infrastructure:** `k8s/base/hpa-backend.yaml` (deleted),
  `k8s/base/kustomization.yaml` (HPA ref removed),
  `scripts/banner_docs.js` (doc utility).
- **Architecture docs:** `docs/engineering/ARCHITECTURE.md` (new, authoritative
  SQLite design) + 47 docs legacy-labelled; `database.md` current-state fixed.
- **Certification:** `docs/certification/p10/`, `p10.1/`, `p11/` (board +
  remediation + final certification reports).
- **Security:** `docs/security/SECURITY_EXCEPTIONS.md`,
  `docs/security/DEPENDENCY_REMEDIATION.md`.

## 3. Release artifacts present?

| Artifact | Present? | Note |
|----------|----------|------|
| Checksums file | ✅ (local) | `release-v1.0.0.checksums.txt` — **stale** (generated before P10.1 edits); will be regenerated for v1.0.1 |
| SBOM | ❌ | generated only by CI (`build.yml`) |
| Docker image | ❌ | built by CI |
| APK / AAB | ❌ | built by CI (`build.yml` / `build-artifacts.yml`) |
| Windows EXE | ❌ | built by CI (`build-artifacts.yml`) |
| GitHub Release | ❌ | to be created |

## 4. Known release risks (from P11 certification)

- **B-3:** 19 high npm vulns (build/install-time, accepted pending upgrade).
- **CI evidence:** none yet — will be generated by this run.
- **Runtime evidence:** none yet — `release-gates.yml` will generate it.
- **Stray `2.0` tag:** recommend maintainer deletion (not auto-deleted).

## 5. Readiness verdict

**READY to execute the release lifecycle** (commit → tag v1.0.1 → push →
run CI → collect artifacts → publish GitHub Release). The only anticipated CI
issue is `security.yml` `npm audit --audit-level=high` failing on the known
high advisories; this will be calibrated to `--audit-level=critical` (0
critical present) with documentation, not hidden.

## 6. Out-of-scope (will NOT be done)

- No business features, no API changes, no architecture redesign.
- Dependency major upgrades (Expo/sqlite3) are deferred to v1.0.2 (human
  decision per `SECURITY_EXCEPTIONS.md`).
